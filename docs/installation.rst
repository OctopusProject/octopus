.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install octopus

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv octopus
    $ pip install octopus